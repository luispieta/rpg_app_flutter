import 'package:flutter/material.dart';

class PersonagemView extends StatelessWidget{
  
  const PersonagemView({super.key});
  
  @override
  Widget build(Object context) {
    return const Center(child: Text("tela personagens"),);
  }

}